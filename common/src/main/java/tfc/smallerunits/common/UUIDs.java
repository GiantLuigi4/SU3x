package tfc.smallerunits.common;

import java.util.Random;
import java.util.UUID;

public class UUIDs {
    public static final UUID SU_REACH_UUID = new UUID(new Random(847329).nextLong(), new Random(426324).nextLong());
}
